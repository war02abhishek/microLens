import PlaceholderScreen from "./PlaceholderScreen";

// PRD §3.1 / §4.4-3: camera view + text/voice entry, reachable in one tap
// from the dashboard. Feeds into MealReviewScreen once AI analysis returns.
export default function MealCaptureScreen() {
  return <PlaceholderScreen title="Meal Capture" note="Camera view + text/voice entry" />;
}
