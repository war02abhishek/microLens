import PlaceholderScreen from "./PlaceholderScreen";

// PRD §3.3 / §4.4-2: animated calorie + macro rings, remaining today,
// day's meals list, "+ Log meal" action, streak indicator.
export default function DashboardScreen() {
  return <PlaceholderScreen title="Dashboard" note="Rings, remaining macros, today's meals, log button" />;
}
