import PlaceholderScreen from "./PlaceholderScreen";

// PRD §3.4 / §4.4-6: curated theme gallery (light/dark/vibrant), custom
// accent color, instant fluid theme switch.
export default function ThemeGalleryScreen() {
  return <PlaceholderScreen title="Personalization / Themes" note="Theme gallery + custom accent color" />;
}
