import PlaceholderScreen from "./PlaceholderScreen";

// PRD §3.2 / §4.4-7: view/edit stats and targets, recalculate goals,
// manual overrides.
export default function ProfileScreen() {
  return <PlaceholderScreen title="Profile & Goals" note="View/edit stats and targets" />;
}
